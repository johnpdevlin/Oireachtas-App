/** @format */

import { Stack } from '@mui/material';

function ProfileIcons(props: { type: string; link: string }) {
	return (
		<>
			<Stack direction='row' sx={{ mt: 2 }}></Stack>
		</>
	);
}

export default ProfileIcons;
